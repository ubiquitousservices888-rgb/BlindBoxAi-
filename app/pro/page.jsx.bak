import Link from "next/link";
export const metadata = { title: "Reseller tools | BlindBoxAI" };
export default function Pro() {
  const link = process.env.NEXT_PUBLIC_STRIPE_PAYMENT_LINK;
  return (
    <main>
      <Link className="crumb" href="/">← All series</Link>
      <h1 className="ptitle">Reseller tools</h1>
      <p style={{maxWidth:"52ch",color:"var(--muted)",marginBottom:"20px"}}>
        For people flipping blind boxes, not chasing them: price alerts when a figure's
        range moves, bulk valuation of a whole lot, and CSV export.
      </p>
      <div className="plan">
        <div className="amt mono">$9<span style={{fontSize:".9rem",color:"var(--muted)"}}>/month</span></div>
        <ul>
          <li>Email price alerts on any figure</li>
          <li>Bulk collection valuation</li>
          <li>CSV export of every range</li>
          <li>Saved set-completion tracking</li>
        </ul>
        {link
          ? <Link className="cta" href={link}>Subscribe →</Link>
          : <p className="nodata">Payment link not set — configure NEXT_PUBLIC_STRIPE_PAYMENT_LINK.</p>}
        <p className="fine" style={{marginTop:"14px"}}>Cancel anytime via the Stripe portal. This tool helps you avoid fakes and overpaying — it will never push you to buy more.</p>
      </div>
    </main>
  );
}
