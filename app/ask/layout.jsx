export const metadata = {
  title: "Mr. Know It All — collectible sold-price research | BlindBoxAI",
  description: "Ask BlindBoxAI about reviewed collectible sold-price evidence. Missing evidence is marked for research instead of guessed.",
};

export default function AskLayout({ children }) {
  return children;
}
